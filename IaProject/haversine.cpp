//
// Created by fchacon on 20-06-17.
//

/*#include <cmath>
#include <string>
using namespace std;

struct City{
    float latitude;
    float longitude;
    string name;
};

City* cities;

int haversine(City city1, City city2) {
    double lat1 = city1.latitude;
    double lat2 = city2.latitude;
    double lon1 = city1.longitude;
    double lon2 = city2.longitude;
    double radiusOfEarth = 4182.44949; // miles, 6371km;
    double dLat = Math.toRadians(lat2 - lat1);
    double dLon = Math.toRadians(lon2 - lon1);
    double a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.cos(Math.toRadians(lat1)) *
                                                         Math.cos(Math.toRadians(lat2)) * Math.sin(dLon / 2) *
                                                         Math.sin(dLon / 2);
    double c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    double distance = radiusOfEarth * c;
    1
    return distance;
}*/